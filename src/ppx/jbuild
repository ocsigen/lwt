(jbuild_version 1)

(library
 ((name ppx_lwt)
  (public_name lwt-ppx)
  (synopsis "Lwt ppx")
  (modules (ppx_lwt))
  (libraries (compiler-libs.common
              ocaml-migrate-parsetree
              ppx_tools_versioned))
  (ppx_runtime_libraries (lwt-core))
  (kind ppx_rewriter)
  (preprocess (pps (ppx_tools_versioned.metaquot_404)))))

