(jbuild_version 1)

(ocamllex (lwt_log_rules))

(library
 ((name lwt_log)
  (public_name lwt.log)
  (synopsis "Logger for Lwt")
  (optional)
  (wrapped false)
  (libraries (lwt))
  (flags (:standard -w +A))))
