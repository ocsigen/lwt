// The wodoc syntax highlighter moved to a single shared, always-current file
// (/doc/wodoc-highlight.js), loaded like the shared CSS. This frozen page still
// references a per-version copy, so just pull in the shared one.
document.write('<script src="/doc/wodoc-highlight.js"><\/script>');
