(jbuild_version 1)

(executable
 ((name main)
  (ocamlopt_flags (:standard (:include ../../src/core/flambda.flag)))
  (libraries (lwttester))))

(alias
 ((name runtest)
  (package lwt)
  (action (run ${exe:main.exe}))))
